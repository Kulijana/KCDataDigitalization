import { GeriatricAssessmentForm } from './geriatric-assessment-form';

describe('GeriatricAssessmentForm', () => {
  it('should create an instance', () => {
    expect(new GeriatricAssessmentForm()).toBeTruthy();
  });
});
